/**
 * HomePage
 * AlphaWaves landing page
 * @component
 */


const HomePage = () => {
  return (
    <div>
      {/* ── 1. Hero ── */}
     

      {/* ── 2. Services Overview ── coming next */}
      {/* ── 3. Why AlphaWaves ──── coming next */}
      {/* ── 4. How We Work ──────── coming next */}
      {/* ── 5. Stats ────────────── coming next */}
      {/* ── 6. Final CTA ────────── coming next */}
    </div>
  );
};

export default HomePage;